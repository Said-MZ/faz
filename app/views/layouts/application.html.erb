<!DOCTYPE html>
<html>

<head>
    <title><%= content_for(:title) || "Faz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Faz">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= tag.link rel: "stylesheet", href: "/assets/builds/tailwind.css", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
</head>

<body>
    <%= render RubyUI::Navbar.new(title: "Hello") do %>
    <%= render RubyUI::NavbarLogo.new(App: "Faz") do %>
    <h1 class="text-xl font-bold">
        Faz
    </h1>
    <% end %>
    <%= render RubyUI::NavbarLinks.new(title: "Hello") do%>
    <%= render RubyUI::NavbarLink.new(href: "#", title: "Home") do %>
    Home
    <% end %>
    <%= render RubyUI::NavbarLink.new(href: "#", title: "About") do %>
    About
    <% end %>
    <%= render RubyUI::NavbarLink.new(href: "#", title: "Contact") do %>
    Contact
    <% end %>
    <% end %>
    <% end %>

    <main class="pt-24 px-4 mx-auto">
        <%= yield %>
    </main>
</body>

</html>